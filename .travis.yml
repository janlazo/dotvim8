language: minimal

addons:
  apt:
    packages:
      - neovim
      - shellcheck
      - vim-nox
    sources:
      - sourceline: ppa:neovim-ppa/unstable
  homebrew:
    update: true
    packages:
      - neovim
      - shellcheck

matrix:
  include:
    - os: linux
      dist: bionic
    - os: linux
      dist: xenial
    - os: osx
      osx_image: xcode11
      rvm: system
    - os: osx
      osx_image: xcode9.4
      rvm: system
    - os: osx
      osx_image: xcode9.2
      rvm: system
    - os: windows

install: ./.ci/install.sh
script: ./.ci/script.sh

notifications:
  email: false
